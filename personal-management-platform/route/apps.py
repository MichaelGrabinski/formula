# This is a minimal Django app config for the new 'route' app
from django.apps import AppConfig

class RouteConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'route'
